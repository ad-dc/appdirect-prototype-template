Follow the audit-prototype skill in `.cursor/skills/audit-prototype/SKILL.md`.

Run typecheck and `npm run ds:audit`. Restricted `@mantine/core` imports fail. Missing `PageContentHeader` and handmade record lists warn.
