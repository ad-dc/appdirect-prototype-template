Follow the prototype workspace skill in `.cursor/skills/prototype-workspace/SKILL.md`.

Use it to scaffold pages, import from `@appdirect/ds-prototype-kit`, keep product widgets in `components/local/`, bump the kit tarball when the design system updates, and run `npm run ds:audit`. `/audit-prototype` is the dedicated check.
