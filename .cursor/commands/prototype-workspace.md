Follow the prototype workspace skill in `.cursor/skills/prototype-workspace/SKILL.md`.

Use it to scaffold pages, import from `@appdirect/ds-prototype-kit`, keep product widgets in `components/cbp/`, and bump the kit tarball when the design system updates.
